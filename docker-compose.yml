
services:

  deepfix-server:
    build: .
    image: deepfix-server:latest
    ports:
      - "8844:8844"
    env_file:
      - .env 
    environment:
      # Configure OpenTelemetry to use Jaeger service in Docker network
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
    volumes:
      - ./server_logs:/logs
    restart: unless-stopped
    depends_on:
      mlflow:
        condition: service_healthy
      jaeger:
        condition: service_started

  mlflow:
    image: ghcr.io/mlflow/mlflow:v3.6.0
    container_name: mlflow
    restart: unless-stopped
    ports:
      - "5000:5000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5000').read()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    command: >
      mlflow server
      --backend-store-uri sqlite:////mlflow/mlflow.db
      --default-artifact-root /mlflow/artifacts
      --host 0.0.0.0
      --port 5000
      --allowed-hosts localhost:5000,localhost,mlflow:5000
    volumes:
      - ./server_mlflow_data:/mlflow

  jaeger:
    image: cr.jaegertracing.io/jaegertracing/jaeger:2.13.0
    container_name: jaeger
    ports:
      - "16686:16686"
      - "4317:4317"
      - "4318:4318"
      - "5778:5778"
      - "9411:9411"
    environment:
      - LOG_LEVEL=debug
    restart: unless-stopped

  #cursor-agent:
  #  build: ./cursor_wrapper
  #  image: cursor-agent:latest
  #  ports:
  #    - "8841:8841"
  #  env_file:
  #    - cursor_wrapper/.env
  #  volumes:
  #    - ./cursor_agent_logs:/logs



